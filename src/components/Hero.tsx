import { CinematicHero } from './CinematicHero'

/**
 * Primary portfolio hero.
 *
 * The cinematic scroll experience is now the default homepage hero rather
 * than an opt-in query-parameter experiment.
 */
export function Hero() {
  return <CinematicHero />
}
